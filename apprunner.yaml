version: 1.0
runtime: nodejs22
build:
  commands: |
    npm install
    npm run build
run:
  runtime-version: 22.14.0
  command: npm run start
network:
  port: 3001
health:
  protocol: HTTP
  path: /health
  interval: 10
  timeout: 5
  healthyThreshold: 1    
  unhealthyThreshold: 10